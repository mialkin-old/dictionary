apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/issuer: letsencrypt-prod
  name: dict
  namespace: mialkin
spec:
  tls:
    - hosts:
        - dict.mialkin.com
      secretName: letsencrypt-dict-tls-secret
  rules:
    - host: dict.mialkin.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dict
                port:
                  number: 80
